'use strict';

/**
 * subscription-feature router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subscription-feature.subscription-feature');
