'use strict';

/**
 * subscription-feature service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscription-feature.subscription-feature');
